import { SliderButtonDirective } from './slider-button.directive';

describe('SliderButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new SliderButtonDirective();
    expect(directive).toBeTruthy();
  });
});
